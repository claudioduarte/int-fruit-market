<div class="col-12 media p-4 pb-2 bg-white mt-4 mb-4 rounded-3 shadow-sm">
  <h5 class="mb-3">🚚 VENDORS</h5>
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Vendor</th>
        <th scope="col">Country</th>
        <% if signed_in? %>
        <th scope="col">Actions</th>
        <% end %>
      </tr>
    </thead>
    <tbody>
      <% @vendors.each do |f| %>
      <tr>
        <th scope="row"><%= f.id %></th>
        <td><%= f.name %></td>
        <td><%= f.country %></td>
        <% if signed_in? %>
        <td><a href="<%= edit_vendor_path f.id %>" class="btn btn-primary">EDIT VENDOR</a></td>
        <% end %>
      </tr>
      <% end %>
    </tbody>
  </table>
  <%== pagy_bootstrap_nav(@pagy_vendors) %>
</div>
